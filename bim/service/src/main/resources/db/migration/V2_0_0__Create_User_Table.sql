CREATE TABLE USERS (
                                 ID BIGINT primary key ,
                                 USER_NAME varchar(30) not null,
                                 FULL_NAME varchar(100)
);
